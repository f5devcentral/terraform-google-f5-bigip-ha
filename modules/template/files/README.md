# Static files

These files are added to all deployments to perform consistent on-boarding of a BIG-IP VE instance on Google Cloud, and
are embedded directly in the cloud-init configuration provided to each instance through the metadata `user-data` key.
